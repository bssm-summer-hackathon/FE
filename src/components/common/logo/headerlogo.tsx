import { styled } from "styled-components";

const Logo = () => {
  return <Container></Container>;
};

export default Logo;

const Container = styled.div`
  width: 6.5rem;
  height: 3.522rem;

  background-size: cover;
  background-repeat: no-repeat;

  background: url("/images/logo/headerlogo.svg");
`;
